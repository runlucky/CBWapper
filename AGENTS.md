# AGENTS.md

## ユーザとの対話
- ユーザの指示が不明瞭な場合は、指示内容をユーザに確認して明確にしてください。
- ユーザの指示が間違っていると思われる場合や、その指示を実行するのがあまりおすすめできない場合は、作業する前にユーザに確認してください。

## コーディングスタイル
- Swiftの API Design Guidelines に準拠してください
- ファイル名と、そのファイルの主となるオブジェクトの名前は一致させること
- アクセス修飾子(private, fileprivate, internal, public)は必ず明示すること。必要最小限のアクセスレベルを選択してください。

## 可読性
- 可読性を最優先にしてください。第三者が素早く誤解なく理解できること。
- 命名は役割と意図が即座に分かる名前にしてください。省略語や曖昧な名前（`data`、`info`、`tmp` など）は避けてください。
- `manager`, `service`, `util`, `info` などの責務が曖昧になる名前は避け、具体的な役割や責務を表す名前を選んでください。
- ネストは浅くしてください。早期リターンを使い、条件分岐の読みやすさを優先してください。
- コメントは「なぜこの実装方法なのか」を補足するために最小限で使ってください。
- メソッドは、メソッド名・引数・戻り値を見ただけで、内部の実装を見なくても何をするのかが分かるようにしてください。

## 責務の明確化
- 各オブジェクトに「何を担当し、何を担当しないか」を明確に持たせてください。
- 1つの型に複数の関心事（UI、永続化、通信、ビジネスロジックなど）を混在させないでください。
- メソッドごとに目的を1つに絞り、前提条件と完了条件が読み取れる実装にしてください。
- 副作用がある処理（状態変更、I/O、通知など）は命名・配置で明確にし、純粋な計算と分離してください。
- 設定値の解決、バリデーション、変換、実行などの工程は役割ごとに分割し、責務を混同しないでください。
- 拡張時に既存責務を壊さないよう、責務単位で変更可能な構造を優先してください。

## 凝集度
- 同じ理由で変更されるコードを近くに配置し、変更理由が異なる処理を同じ型に入れないでください。
- 型・メソッド・ファイルは、それぞれ1つの中心的関心にまとまるようにしてください。
- パラメータの受け渡しが増えすぎる場合は、責務境界やデータ構造を見直してください。
- ある機能に必要な知識（型、ロジック、変換規則）が分散しすぎないようにしてください。
- 凝集度を優先し、関連の薄い共通化よりも、文脈に沿ったローカルな実装を選んでください。
- 過剰設計は避け、既存コードとのバランスを見て必要以上の抽象化は行わないでください。

## Git操作
- コミットメッセージは日本語で記載してください
- ブランチ名は`ai/`のプレフィックスを付けてください
- ブランチ名は半角英数字とハイフンのみ使用し、最大でも3単語からなる短い名前にしてください。
